<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The reader WebView loads real pages. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClipperDev">

        <!-- Launcher. Almost empty by design: the app is driven from the share sheet. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- M1.1 — the real entry point. No UI: it resolves the URL and hands off.

             `noHistory` drops this invisible activity from the back stack the moment it finishes.
             There is deliberately NO `excludeFromRecents` here: this activity is the *root* of the
             task the share creates, so that attribute governs the whole task, taking ReaderActivity
             with it. The reader then vanishes from the app switcher and cannot be returned to,
             which reads as "the app closed when I switched apps" even though the process is still
             alive. Measured on the Find N6, 2026-09-01. Do not re-add it to keep the trampoline out
             of Recents — `noHistory` already does that job. -->
        <activity
            android:name=".share.ShareReceiverActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.Trampoline">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- M1.2 — the reader. configChanges keeps the WebView (and the rendered reader) alive
             across a fold/unfold on the Find N6 rather than recreating the activity; M1.8 is the
             pass that proves it. -->
        <activity
            android:name=".reader.ReaderActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode" />
    </application>

</manifest>
