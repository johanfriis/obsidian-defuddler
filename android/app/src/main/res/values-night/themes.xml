<resources>
    <!-- Dark half of the pair. See values/themes.xml for why isLightTheme is explicit. -->
    <style name="Theme.ClipperDev" parent="android:Theme.Material.NoActionBar">
        <item name="android:isLightTheme">false</item>
    </style>

    <style name="Theme.Trampoline" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
