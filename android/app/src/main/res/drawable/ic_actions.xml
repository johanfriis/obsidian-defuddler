<!-- Lucide "plus" — the collapsed shell FAB. Neutral on purpose: it stands for "the app's
     actions", not for either of them, and rotates 45° into a close mark when the menu opens. -->
<!-- strokeColor is a placeholder: Compose's Icon tints this from
     LocalContentColor, so the value here never reaches the screen. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24">
    <path android:strokeColor="#FF000000" android:strokeWidth="2"
        android:strokeLineCap="round" android:strokeLineJoin="round"
        android:pathData="M5,12h14" />
    <path android:strokeColor="#FF000000" android:strokeWidth="2"
        android:strokeLineCap="round" android:strokeLineJoin="round"
        android:pathData="M12,5v14" />
</vector>
