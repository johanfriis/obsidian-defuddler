<!-- Deliberately darker than Obsidian's own icon: md.obsidian sits on the same home screen, and
     two violet gems would be a coin toss every time. Ours is a light gem on near-black; theirs is
     the reverse. (D34 is what allows the mark here at all — personal use, never distributed.) -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#1E1A2B"
        android:pathData="M0,0h108v108h-108z" />
</vector>
