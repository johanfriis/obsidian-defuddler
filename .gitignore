# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Android / Gradle
android/.gradle/
android/build/
android/*/build/
local.properties
android/.idea/
android/*.iml
android/.kotlin/
*.apk
*.aab

# IDE
.vscode/
.idea/

# Env / secrets
.env
.env.*

# jsbridge build byproducts (the bundle itself IS committed — see playbook §14)
jsbridge/build-meta.json

# Scratch bundle the jsbridge tests build into (never the committed asset).
jsbridge/.tmp/

# M2.0 spike build output (the harness in jsbridge/spike/ IS committed — see playbook §2).
jsbridge/.spike-ui/
