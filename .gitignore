# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Build output. main.js is a release asset, attached to the GitHub release, never committed.
main.js
main.js.map
*.tgz
.tmp/

# IDE
.vscode/
.idea/

# Env / secrets
.env
.env.*

# Android build outputs. The app itself lives on the android-reader branch; what is left here is
# untracked Gradle and SDK state, kept so a branch switch does not rebuild 84 MB from scratch.
android/
