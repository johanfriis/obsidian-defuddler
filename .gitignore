# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Build output. main.js is a release asset, attached to the GitHub release, never committed.
main.js
main.js.map
*.tgz
.tmp/

# IDE
.vscode/
.idea/

# Env / secrets
.env
.env.*

# Leftover Gradle and SDK state from the Android app this project began as. The app itself lives on
# the android-reader branch of the obsidian-reader repository; this is untracked local build cache,
# kept only so switching to that branch does not rebuild it from scratch.
android/
