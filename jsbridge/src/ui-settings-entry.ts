// Entry for android/app/src/main/assets/ui/settings.js — upstream's settings page and template
// editor (D31; this is what retired D16).

import { installBackground } from './background';

installBackground();

import '../vendor/obsidian-clipper/src/core/settings';
